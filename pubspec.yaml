name: gestureappflutter
description: Controlla il volume di sistema inclinando il telefono a destra o sinistra.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sensors_plus: ^4.0.2
  volume_controller: ^2.0.7

flutter:
  uses-material-design: true